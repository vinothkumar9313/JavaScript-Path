let num1 = 26,
  num2 = 22,
  num3 = 12;
 let sub = num1 - num3           //subtraction
  console.log(sub);

  let add = num1+num2             //addition
  console.log(add);

  let rem = num1%num3              //reminder
  console.log(rem);


  let math = (num1+num2)*num3
  console.log(math);